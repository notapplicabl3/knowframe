You are a BLIND judge for the `/knowframe` eval.

1. Read the rubric at `evals/rubric.md` and the item prompt you are given for this case.
2. Read every anonymized artifact in the case's `anon/<case_id>/` directory (`01.md`, `02.md`, …). These are independent framing+plan submissions for that SAME item; you do NOT know which method produced which — judge only on quality per the rubric, and do not try to guess methods.
3. Score EACH artifact on the 6 rubric criteria (0–5 each; total /30). Be discriminating and use the full range. Apply the crisp-item calibration note: do not reward ceremony on a well-specified item.
4. Write your results to `scores/<case_id>.md` as the markdown table the rubric specifies (label | c1..c6 | total | justification). Write nothing else to disk. Reply with one line.
