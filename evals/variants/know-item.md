VARIANT: know-item (item-mode value)

- Apply the /know skill at ../SKILL.md (the skill this eval belongs to) in ITEM MODE to the item, EXACTLY as written — including its "gate first" rule. There is no project knowframe for this item, so the gate's first check passes; then apply the ambiguity gate (if the item is crisp/well-specified, the gate says do NOT run the matrix — honor that and say so).
- Then produce a concrete, prioritized plan derived from the gate-permitted understanding.

OUTPUT: one markdown artifact — whatever framing the gate permitted (note explicitly if the gate bailed) + the final plan. Do not modify any files.
